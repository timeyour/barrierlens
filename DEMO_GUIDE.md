# BarrierLens Demo 演示指南

> 3 分钟比赛 / 路演演示路径 · Gemma 4 Hackathon 2026

## 演示前准备

1. 打开在线 Demo：`https://barrierlens.vercel.app/#tool`（或本地 `http://localhost:3000/#tool`）
2. 建议使用 **Chrome / Safari 手机或笔记本**，全屏展示
3. 现场网络不稳时，**全程使用「演示样例」**，不依赖外部 API

## 3 分钟演示脚本

| 时间 | 步骤 | 操作 | 讲解要点 |
|------|------|------|----------|
| 0:00 | 1. 首页口径 | 滚动至 Hero / `#story` | **看见问题不难，留下证据才难** · 不是 AI 看图 Demo，而是证据链 |
| 0:30 | 2. 进入工具 | 点击「开始记录证据」→ `#tool` | 照片 → 结构化证据 → 时间线 → 导出 / 复查 |
| 0:45 | 3. 演示样例 | 点 **「使用演示样例」** | 比赛兜底模式，约 2 秒，不依赖 Gemma API |
| 1:00 | 4. 填写地点 | 确认路名已填（样例可改） | 证据必须可定位、可复查 |
| 1:15 | 5. 选择模式 | 切换「公众记录 / 物业自查」 | 同一结构化 JSON，不同输出文本 |
| 1:30 | 6. 生成证据 | 点「生成报告」 | Gemma 4 输出 sceneType、riskLevel、evidenceSummary 等 |
| 2:00 | 7. 结构化字段 | 展开结果页「结构化证据字段」 | 强调 **analysisSource=gemma 或 demo-mock**，需人工复核 |
| 2:20 | 8. 时间线 | 滚动至 `#records` | 记录已自动归档；可点「加载演示样例（10 条）」 |
| 2:40 | 9. 导出 | 点「导出 Markdown」或「导出 PDF」 | 含人工复核声明，可递出 / 存档 |
| 2:55 | 10. 收尾 | 强调复查 | AI 辅助，不替代执法与专业验收 |

## 演示样例模式（P2 兜底）

### 工具页单条分析

1. 工具页 → **使用演示样例**
2. 确认出现「当前：样例图（演示约 2 秒出结果）」
3. 填写地点 → 继续 → 生成报告
4. 结果页 `analysisSource=demo-mock`（或线上缓存命中时为 `gemma`）

### 时间线批量样例

1. 滚动至 **证据时间线**（`#records`）
2. 点击 **加载演示样例（10 条）**
3. 可筛选状态、展开详情、**生成巡检报告** 预览

## 真实上传模式（可选）

> 仅在网络稳定、已配置 `GEMINI_API_KEY` 时演示

1. 上传现场照片（建议 < 2MB）
2. 填写地点 → 选择模式 → 生成
3. 结果页应显示 `analysisSource=gemma`
4. 核对结构化字段是否与照片一致

**注意：** Vercel Hobby 约 60 秒超时，大照片可能失败。答辩优先演示样例。

## 人工复核（必讲一句）

> AI 识别结果仅作为辅助参考，最终问题确认、责任划分和整改验收应由人工复核完成。

## 常见问题

| 现象 | 处理 |
|------|------|
| 分析超时 | 清除 → 使用演示样例 |
| 结果与照片不符 | 说明 demo-mock / 需人工复核 |
| 时间线为空 | 生成一条记录，或加载 10 条演示样例 |
| 手机底部按钮被挡 | 使用底部导航「记录 / 时间线」 |

## 相关文档

- [README.md](./README.md) — 项目说明
- [SUBMISSION_NOTES.md](./SUBMISSION_NOTES.md) — 提交清单
- [docs/DEMO_VIDEO_SCRIPT.md](./docs/DEMO_VIDEO_SCRIPT.md) — 视频脚本
